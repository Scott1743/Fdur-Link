<!--<table>-->
  <!--<thead>-->
  <!--<tr>-->
    <!--<th>Name</th>-->
    <!--<th>Description</th>-->
    <!--<th>State</th>-->
    <!--<th>Is_public?</th>-->
    <!--<th>ajax</th>-->
  <!--</tr>-->
  <!--</thead>-->

  <!--<tbody>-->
  <!--<% @projects.each do |project| %>-->
      <!--<tr>-->
        <!--<td><%= project.name %></td>-->
        <!--<td><%= project.description %></td>-->
        <!--<td><%= project.state %></td>-->
        <!--<td><%= project.is_public %></td>-->
        <!--<td><%= 1%></td>-->
        <!--<td><%= link_to 'Show', project %></td>-->
        <!--<td><%= link_to 'Edit', edit_project_path(@user,project) %></td>-->
        <!--<td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      <!--</tr>-->
  <!--<% end %>-->
  <!--</tbody>-->
</table>
<div class="projects">
<ul>
  <li class="can_hover" data-flag="click_hidden">
    <div>
      <span class="new_project">
        <b style="display: block;font-size: 100px;font-weight: 600;margin-top: 50px;line-height: 120px">+</b>
        新的启程让生活更多彩
      </span>
    </div>
  </li>
  <li id="new_project_form" >
    <div>
      <%= form_for :project,url: projects_path,methode: :post do |f| %>
          <div>
            <span>计划名</span>
            <%= f.text_field :name %><br/>
            <span>描述</span>
            <%= f.text_area :description %><br/>
            <span>图片链接</span>
            <%= f.text_field :image %><br/>
            <%= f.radio_button :is_public, 'false', checked: true %>
            <%= f.label :is_public, "private", value: 'false'%>
            <%= f.radio_button :is_public, 'true' %>
            <%= f.label :is_public, "public", value: 'true'%>
            <%= f.submit %>
          </div>
      <% end %>
    </div>
  </li>
<% @projects.each_with_index do |project,index| %>
  <% if (index%3 == 2) %>
  <%= link_to project do %>
  <li class="can_hover">
    <div>
      <% if project.image.nil? %>
          <span><%= image_tag 'default.jpg' ,style: 'width: 310px; max-height:500px; overflow:hidden;'%></span>
      <% else %>
          <span><%= image_tag project.image ,style: 'width: 310px; max-height:500px; overflow:hidden;'%></span>
      <% end %>
      <span class="project_span project_name <%= project.is_public ? 'state_public' : 'state_private' %>" ><%= project.name %></span><br/>
      <span class="project_span project_description" ><%= project.description %></span>
      <span class="project_span project_milestone">milestone</span>
    </div>
    <span class="project_span project_comments">comments<br/>comments</span>
  </li>
  <% end %>
  <% end %>
<% end %>
</ul>
<ul>
<% @projects.each_with_index do |project,index| %>
  <% if (index%3 == 0) %>
  <%= link_to project do %>
  <li class="can_hover">
    <div>
      <% if project.image.nil? %>
          <span><%= image_tag 'default.jpg' ,style: 'width: 310px; max-height:500px; overflow:hidden;'%></span>
      <% else %>
          <span><%= image_tag project.image ,style: 'width: 310px; max-height:500px; overflow:hidden;'%></span>
      <% end %>
      <span class="project_span project_name <%= project.is_public ? 'state_public' : 'state_private' %>" ><%= project.name %></span><br/>
      <span class="project_span project_description" ><%= project.description %></span>
      <span class="project_span project_milestone">milestone</span>
    </div>
    <span class="project_span project_comments">comments<br/>comments</span>
  </li>
  <% end %>
  <% end %>
<% end %>
</ul>
<ul>
<% @projects.each_with_index do |project,index| %>
  <% if (index%3 == 1) %>
    <%= link_to project do %>
      <li class="can_hover">
        <div>
          <% if project.image.nil? %>
            <span><%= image_tag 'default.jpg' ,style: 'width: 310px; max-height:500px; overflow:hidden;'%></span>
          <% else %>
            <span><%= image_tag project.image ,style: 'width: 310px; max-height:500px; overflow:hidden;'%></span>
          <% end %>
          <span class="project_span project_name <%= project.is_public ? 'state_public' : 'state_private' %>" ><%= project.name %></span><br/>
          <span class="project_span project_description" ><%= project.description %></span>
          <span class="project_span project_milestone">milestone</span>
        </div>
        <span class="project_span project_comments">comments<br/>comments</span>
      </li>
    <% end %>
  <% end %>
<% end %>
</ul>

</div>
